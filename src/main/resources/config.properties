# Cinema Booking System - Database Configuration
# Copy this file and rename to config.properties
# Add config.properties to .gitignore to keep passwords secure

# Database Connection
db.url=jdbc:mysql://localhost:3306/cinema_booking
db.username=root
db.password=th0m4sj4m3s

# Connection Pool Settings
db.pool.max.size=10
db.pool.min.idle=2
db.pool.connection.timeout=30000
db.pool.idle.timeout=600000
db.pool.max.lifetime=1800000
db.pool.leak.detection=60000

# Application Settings
app.name=Cinema Booking System
app.version=1.0.0
app.environment=development

# Logging
log.level=INFO
log.file.path=logs/application.log

# Session Settings
session.timeout=3600000
session.cleanup.interval=300000

# Seat Lock Settings
seat.lock.duration=600000
seat.lock.cleanup.interval=300000

# Cache Settings
cache.movies.ttl=1800000
cache.showtimes.ttl=600000

# Email Settings (for future use)
email.enabled=false
email.host=smtp.gmail.com
email.port=587
email.username=your-email@gmail.com
email.password=your-email-password

# IMDB API
imdb.api.key=451f7c8
imdb.api.url=https://www.omdbapi.com/
