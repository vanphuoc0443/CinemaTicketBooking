<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Label?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ScrollPane?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.image.ImageView?>
<?import javafx.geometry.Insets?>

<AnchorPane prefWidth="360" prefHeight="780"
            minWidth="360" minHeight="780"
            maxWidth="360" maxHeight="780"
            xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="fxcontroller.HistoryController"
            style="-fx-background-color: linear-gradient(to bottom, #020617, #0B1120);">

    <!-- Main Layout -->
    <VBox spacing="18"
          AnchorPane.topAnchor="0"
          AnchorPane.bottomAnchor="0"
          AnchorPane.leftAnchor="0"
          AnchorPane.rightAnchor="0">

        <!-- Safe Area (giống mobile notch padding) -->
        <padding>
            <Insets top="28" right="16" bottom="20" left="16"/>
        </padding>

        <!-- Header (Chuẩn app cinema) -->
        <HBox spacing="12" alignment="CENTER_LEFT" prefHeight="50">
            <Button fx:id="btnBack"
                    prefWidth="42" prefHeight="42"
                    text="←"
                    style="
                        -fx-background-color:#1E293B;
                        -fx-background-radius:14;
                        -fx-text-fill:#38BDF8;
                        -fx-font-size:18;
                        -fx-cursor:hand;
                    "/>

            <VBox>
                <Label text="Lịch sử đặt vé"
                       style="
                           -fx-text-fill:white;
                           -fx-font-size:22;
                           -fx-font-weight:bold;
                       "/>
                <Label text="Vé đã thanh toán"
                       style="-fx-text-fill:#94A3B8; -fx-font-size:12;"/>
            </VBox>
        </HBox>

        <!-- Ticket List -->
        <ScrollPane fitToWidth="true"
                    prefHeight="700"
                    hbarPolicy="NEVER"
                    vbarPolicy="AS_NEEDED"
                    style="-fx-background:transparent; -fx-background-color:transparent;">

            <VBox fx:id="historyContainer" spacing="16">
                <padding>
                    <Insets top="5" right="4" bottom="20" left="4"/>
                </padding>

                <!-- Cinema Ticket Card (TỈ LỆ CHUẨN MOBILE) -->
                <HBox spacing="14"
                      prefWidth="328"
                      style="
                        -fx-background-color: linear-gradient(to right, #1E293B, #0F172A);
                        -fx-background-radius:20;
                        -fx-border-radius:20;
                        -fx-border-color:#334155;
                        -fx-effect:dropshadow(gaussian, rgba(0,0,0,0.6), 16, 0.3, 0, 8);
                      ">

                    <padding>
                        <Insets top="16" right="16" bottom="16" left="16"/>
                    </padding>

                    <!-- Poster chuẩn tỉ lệ 2:3 -->
                    <ImageView fitWidth="72" fitHeight="108"
                               smooth="true"
                               preserveRatio="false"
                               style="
                                   -fx-background-radius:14;
                                   -fx-effect:dropshadow(gaussian, rgba(0,0,0,0.5), 8, 0.3, 0, 4);
                               "/>

                    <!-- Ticket Info -->
                    <VBox spacing="7" prefWidth="210">

                        <!-- Movie Title (2 dòng max như app thật) -->
                        <Label text="Avengers: Endgame"
                               wrapText="true"
                               maxWidth="210"
                               style="
                                   -fx-text-fill:white;
                                   -fx-font-size:16;
                                   -fx-font-weight:bold;
                               "/>

                        <Label text="⏰ 20:00 | Phòng 5"
                               style="-fx-text-fill:#94A3B8; -fx-font-size:13;"/>

                        <Label text="🎟 Ghế: A1, A2"
                               style="-fx-text-fill:#E2E8F0; -fx-font-size:13;"/>

                        <!-- Divider line (giống vé điện tử) -->
                        <Label text="────────────────"
                               style="-fx-text-fill:#334155; -fx-font-size:10;"/>

                        <!-- Price + Status Row -->
                        <HBox alignment="CENTER_LEFT" spacing="10">
                            <Label text="180,000đ"
                                   style="
                                       -fx-text-fill:#FACC15;
                                       -fx-font-size:17;
                                       -fx-font-weight:bold;
                                   "/>

                            <Label text="ĐÃ THANH TOÁN"
                                   style="
                                       -fx-background-color:#22C55E;
                                       -fx-text-fill:white;
                                       -fx-padding:3 10 3 10;
                                       -fx-background-radius:10;
                                       -fx-font-size:10;
                                       -fx-font-weight:bold;
                                   "/>
                        </HBox>

                    </VBox>

                </HBox>

            </VBox>
        </ScrollPane>

    </VBox>
</AnchorPane>