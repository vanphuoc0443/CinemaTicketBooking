<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<StackPane styleClass="page-container"
           prefWidth="1000" prefHeight="700"
           xmlns="http://javafx.com/javafx/25"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="fxcontroller.TicketController">

    <VBox styleClass="ticket-card" spacing="14" alignment="CENTER" maxWidth="520">
        <padding><Insets top="28" right="36" bottom="28" left="36"/></padding>

        <!-- Celebration -->
        <Label text="🎉" styleClass="celebration-icon"/>
        <Label text="Đặt vé thành công!" styleClass="heading-xl"/>
        <Label text="Vé của bạn đã được xác nhận" styleClass="text-secondary"/>

        <!-- Gradient Divider -->
        <Region styleClass="gradient-divider" maxWidth="320"/>

        <!-- Ticket Details -->
        <HBox spacing="30" alignment="CENTER">
            <VBox alignment="CENTER" spacing="4">
                <Label text="🎬 Phim" styleClass="info-label"/>
                <Label fx:id="movieTicketLabel" text="—" styleClass="info-value"/>
            </VBox>
            <VBox alignment="CENTER" spacing="4">
                <Label text="🕐 Suất" styleClass="info-label"/>
                <Label fx:id="showtimeTicketLabel" text="—" styleClass="info-value"/>
            </VBox>
        </HBox>

        <HBox spacing="30" alignment="CENTER">
            <VBox alignment="CENTER" spacing="4">
                <Label text="🎟 Ghế" styleClass="info-label"/>
                <Label fx:id="seatsTicketLabel" text="—" styleClass="info-value" wrapText="true"/>
            </VBox>
            <VBox alignment="CENTER" spacing="4">
                <Label text="💰 Tổng tiền" styleClass="info-label"/>
                <Label fx:id="priceTicketLabel" text="—" styleClass="price-tag"/>
            </VBox>
        </HBox>

        <Label fx:id="ticketLabel" styleClass="text-body" wrapText="true"/>

        <!-- QR Code -->
        <Region styleClass="gradient-divider" maxWidth="320"/>
        <Label text="📱 Quét mã QR tại quầy" styleClass="text-secondary"/>
        <ImageView fx:id="qrCodeImage" fitWidth="160" fitHeight="160" preserveRatio="true"
                   style="-fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 8, 0, 0, 2);"/>

        <!-- Gradient Divider -->
        <Region styleClass="gradient-divider" maxWidth="320"/>

        <!-- Action Buttons -->
        <HBox spacing="12" alignment="CENTER">
            <Button text="🏠 Đặt vé khác" onAction="#goHome"
                    styleClass="btn-gold" prefWidth="180" prefHeight="40"/>
            <Button text="📜 Lịch sử" onAction="#goHistory"
                    styleClass="btn-outline" prefWidth="180" prefHeight="40"/>
        </HBox>
    </VBox>
</StackPane>
